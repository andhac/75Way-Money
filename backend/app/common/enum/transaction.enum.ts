export enum TransactionStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum TransactionType {
    FUNDING = "funding",
    TRANSFER = "transfer",
    WITHDRAWAL = "withdrawal",
}



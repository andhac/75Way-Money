import { Box } from "@mui/material";
import LoginForm from "../component/LoginForm.tsx";

const Login = () => {
    return (
        <Box>
            <LoginForm />
        </Box>
    );
};

export default Login;